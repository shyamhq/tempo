{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Isomorphic: uses web-platform globals (fetch, AbortSignal) available in
    // both the browser and Node >= 20. (EventSource comes from the npm package.)
    "lib": ["ES2023", "DOM"]
  },
  "include": ["src/**/*.ts"]
}
